apiVersion: v1
kind: Namespace
metadata:
  name: myapp-dev
  labels:
    # Labels are key-value pairs used for selection, filtering, and policy targeting
    environment: dev
    team: myapp
    managed-by: tutorial-t01